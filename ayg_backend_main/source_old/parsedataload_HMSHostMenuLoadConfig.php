<?php

// @todo - not needed now

	$partner = "hmshost";
	///////////////////////////////////////////////////////////////////////////////////////
	// STEP 1 - Create new menu file by running parsedataload_MenuGenerateHMSHost.php
	// BWI - Brix n Vine
	// $propertyId = 83; // Airport ID from HMSHost
	// $revenueCenterId = 90; // Retailer ID from HMSHost
	// $retailerUniqueId = '03ebb2b93085bee17756150d39a54cf3'; // Load retailer via Retailers file first
	// $airportIataCode = 'BWI';
	// $menuFileNameNewPrefix = 'HMSHost-Brix-n-Vine';
	// $menuFileNameOldPrefix = 'HMSHost-Brix-n-Vine-Prod';
	// $menuFileNameMergePrefix = 'HMSHost-Brix-n-Vine-Merge';

	/////////////////////////////////////////////////////////////////////////////////////////
	// STEP 2 - Run this file
	/////////////////////////////////////////////////////////////////////////////////////////

	// Where to create the new menu pull
	// $newFiles = '<path>\\' . $airportIataCode . '\data\Menus\\' . $menuFileNameNewPrefix;

	// // Existing prod menu
	// // $menuFileNameOldPrefix = 'HMSHost-Bahama-Breeze';
	// $oldFiles = '<path>' . $airportIataCode . '\data\Menus\\' . $menuFileNameOldPrefix;

	// // Merged menus
	// $mergeFiles = '<path>' . $airportIataCode . '\data\Menus\\' . $menuFileNameMergePrefix;

	// $promoteLocation = '<path>' . $airportIataCode . '\data\Menus\HMSHost';

	// $fileListNew = array(
	// 				"0" => $newFiles . ' - items.csv',
	// 				"1" => $newFiles . ' - modifiers.csv',
	// 				"2" => $newFiles . ' - options.csv',
	// 				"3" => $newFiles . ' - itemtimes.csv',
	// 			);

	// $fileListProd = array(
	// 				"0" => $oldFiles . ' - items.csv',
	// 				// TAG
	// 				"1" => $oldFiles . ' - modifiers.csv',
	// 				"2" => $oldFiles . ' - options.csv',
	// 				"3" => $oldFiles . ' - itemtimes.csv',
	// 			);
		
	// $fileListMerge = array(
	// 				"0" => $mergeFiles . ' - items.csv',
	// 				"1" => $mergeFiles . ' - modifiers.csv',
	// 				"2" => $mergeFiles . ' - options.csv',
	// 				// TAG
	// 				"3" => $mergeFiles . ' - itemtimes.csv',
	// 			);

?>
